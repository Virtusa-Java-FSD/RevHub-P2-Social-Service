server:
  port: 8082

spring:
  application:
    name: social-service
  config:
    import: optional:configserver:http://localhost:8888
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB
